{
  "name": "vanilla-drag-movable",
  "version": "1.0.0",
  "description": "Drag and move behavior for DOM elements",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "repository": "https://github.com/tai2/vanilla-drag-movable",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prepare": "yarn test && yarn build",
    "format": "prettier --write {src,tests,demo}/*.{ts,html}",
    "test": "jest --coverage"
  },
  "author": "Taiju Muto <hello@tai2.net>",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^24.0.25",
    "codecov": "^3.6.1",
    "jest": "^24.9.0",
    "prettier": "^1.19.1",
    "ts-jest": "^24.3.0",
    "typescript": "^3.7.4"
  }
}
